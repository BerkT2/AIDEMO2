<?xml version="1.0" encoding="UTF-8"?>
<ValidationRule xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>Tax_Number_Format</fullName>
    <active>true</active>
    <description>Tax Number must be 10 digits (corporate) or 11 digits (individual). Cannot be any other length.</description>
    <errorConditionFormula>AND(
  NOT(ISBLANK(Tax_Number__c)),
  NOT(OR(
    LEN(Tax_Number__c) = 10,
    LEN(Tax_Number__c) = 11
  ))
)</errorConditionFormula>
    <errorMessage>Tax Number must be exactly 10 digits (corporate) or 11 digits (individual Turkish National ID).</errorMessage>
</ValidationRule>
